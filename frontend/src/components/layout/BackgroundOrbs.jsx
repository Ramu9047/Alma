import React from 'react';

/**
 * BackgroundOrbs Component (Deprecated)
 * Removed per Alma visual identity specification (no background drifting orbs).
 */
export default function BackgroundOrbs() {
  return null;
}
